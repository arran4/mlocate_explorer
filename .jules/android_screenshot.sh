#!/bin/bash
# We need to run flutter for android but we don't have an emulator running.
# Let's check if there are any emulators available or if we can use a similar approach
# flutter devices
# But wait, we can just build the android app, and maybe run it on a headless emulator?

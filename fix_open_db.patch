--- lib/screens/file_picker_screen.dart
+++ lib/screens/file_picker_screen.dart
@@ -211,10 +211,15 @@
     ];

     String? foundPath;
+    String? errorMessage;
     for (final p in possiblePaths) {
-      if (await File(p).exists()) {
-        foundPath = p;
-        break;
+      final file = File(p);
+      try {
+        if (await file.exists()) {
+          final openedFile = await file.open(mode: FileMode.read);
+          await openedFile.close();
+          foundPath = p;
+          break;
+        }
+      } on FileSystemException catch (e) {
+        errorMessage = "Permission denied or error reading $p: ${e.message}";
       }
     }

@@ -228,8 +233,9 @@
     } else {
       if (mounted) {
         ScaffoldMessenger.of(context).showSnackBar(
-          const SnackBar(
-            content: Text('Could not find system database (/var/lib/mlocate/mlocate.db or plocate.db)'),
+          SnackBar(
+            content: Text(errorMessage ??
+                'Could not find system database (/var/lib/mlocate/mlocate.db or plocate.db)'),
           ),
         );
       }
